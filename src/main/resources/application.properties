spring.application.name=spring-ai-mpc-hello-world

spring.main.web-application-type=none
spring.ai.mcp.server.name=daebecodin-pc-list
spring.ai.mcp.server.version=0.0.1

# NOTE: You must disable the banner and the console logging
# to allow the STDIO transport to work !!!
spring.main.banner-mode=off

# Disable all console logging to prevent interference with MCP JSON protocol
logging.level.root=OFF
logging.level.org.springframework=OFF
logging.level.com.daebecodin=OFF

# Redirect all logging to stderr if needed for debugging
# logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} – %msg%n
# logging.config=classpath:logback-spring.xml
